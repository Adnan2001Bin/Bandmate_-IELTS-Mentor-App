import { Placeholder } from '@/components/layout';

export default function MockReportScreen() {
  return (
    <Placeholder
      backable
      kicker="Mock"
      title="Band report"
      phase="Phase 09"
      summary="Overall band with the four section scores, the criterion breakdown, and what to fix next."
    />
  );
}
