import { Placeholder } from '@/components/layout';

export default function WeaknessesScreen() {
  return (
    <Placeholder
      backable
      kicker="Progress"
      title="Weaknesses"
      phase="Phase 09"
      summary="What the AI has detected across your work, ranked by how much band it is costing you."
    />
  );
}
