import { Placeholder } from '@/components/layout';

export default function HistoryScreen() {
  return (
    <Placeholder
      backable
      kicker="Progress"
      title="History"
      phase="Phase 09"
      summary="Every practice session and mock test in date order, with the band each one produced."
    />
  );
}
