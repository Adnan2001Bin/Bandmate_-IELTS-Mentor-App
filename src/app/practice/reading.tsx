import { Placeholder } from '@/components/layout';

export default function ReadingScreen() {
  return (
    <Placeholder
      backable
      kicker="Practice"
      title="Reading"
      phase="Phase 07B"
      summary="Passages with the question panel, answer locating that highlights the source sentence, and timed sets."
    />
  );
}
