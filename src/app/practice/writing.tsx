import { Placeholder } from '@/components/layout';

export default function WritingScreen() {
  return (
    <Placeholder
      backable
      kicker="Practice"
      title="Writing"
      phase="Phase 07C"
      summary="Task 1 and Task 2 with the editor, word counter, auto-save, and the criterion-level AI evaluation."
    />
  );
}
