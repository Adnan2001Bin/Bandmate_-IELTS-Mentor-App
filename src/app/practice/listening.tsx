import { Placeholder } from '@/components/layout';

export default function ListeningScreen() {
  return (
    <Placeholder
      backable
      kicker="Practice"
      title="Listening"
      phase="Phase 07A"
      summary="Section practice, the audio player with transcript and replay, every question type, and the results breakdown."
    />
  );
}
