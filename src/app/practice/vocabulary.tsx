import { Placeholder } from '@/components/layout';

export default function VocabularyScreen() {
  return (
    <Placeholder
      backable
      kicker="Practice"
      title="Vocabulary"
      phase="Phase 07E"
      summary="Topic sets, word detail with collocations, and the spaced-repetition review queue."
    />
  );
}
