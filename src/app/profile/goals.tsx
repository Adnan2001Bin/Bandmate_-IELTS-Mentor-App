import { Placeholder } from '@/components/layout';

export default function GoalsScreen() {
  return (
    <Placeholder
      backable
      kicker="Profile"
      title="Goals"
      phase="Phase 10"
      summary="Target band, test date, IELTS type and daily study time, with the plan rebuilt when they change."
    />
  );
}
